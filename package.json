{
  "name": "pob-blog",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint src",
    "start": "run-p start:**",
    "start:hugo": "hugo serve -vw",
    "preview": "run-p preview:**",
    "preview:hugo": "npm run start:hugo -- -D -F",
    "prebuild": "rimraf dist",
    "build": "npm run build:hugo",
    "build:preview": "npm run build:hugo:preview",
    "build:hugo": "hugo -v",
    "build:hugo:preview": "npm run build:hugo -- -D -F"
  },
  "devDependencies": {
    "hugo-bin": "^0.46.3",
    "node-sass": "^4.10.0",
    "npm-run-all": "^4.1.5"
  },
  "hugo-bin": {
    "buildTags": "extended"
  }
}
